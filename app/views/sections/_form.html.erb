<%= form_for(section) do |f| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section_cd %>
    <%= f.text_field :section_cd %>
  </div>

  <div class="field">
    <%= f.label :section_name %>
    <%= f.text_field :section_name %>
  </div>

  <div class="field">
    <%= f.label :parent_cd %>
    <%= f.text_field :parent_cd %>
  </div>

  <div class="field">
    <%= f.label :del_flg %>
    <%= f.number_field :del_flg %>
  </div>

  <div class="field">
    <%= f.label :remark %>
    <%= f.text_area :remark %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
